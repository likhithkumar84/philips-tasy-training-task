import TasyDOMHandler from "../../labs/tasyDOMHandler";
//import { getHandler } from "../../labs/helpers";

export default class wMyComponentDomHandler extends TasyDOMHandler {
  constructor(element, handler, store) {
    super(element, handler, store);
  }
}
